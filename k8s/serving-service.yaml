apiVersion: v1
kind: Service
metadata:
  name: ml-serving-service
  namespace: ml-training
  labels:
    project: mlops-pytorch-pipeline
    app: ml-serving
spec:
  type: ClusterIP        
  selector:
    app: ml-serving
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080